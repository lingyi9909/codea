{
  "name": "@codea/enterprise-plugins",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Codea V1 enterprise security + custom tools plugin bundle (self-contained ESM, target bun)",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist --format esm --target bun",
    "check": "bun test && bun run build"
  },
  "dependencies": {
    "zod": "4.1.8"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
